name: env-secrets
secrets:
  DEMO_TOKEN:
    from: env
    required: true
jobs:
  demo:
    steps:
      - name: Mask secret output
        env:
          TOKEN: "${secrets.DEMO_TOKEN}"
        # Demonstration only; applications should avoid printing secrets.
        run: python -c 'import os; print("Token:", os.environ["TOKEN"])'
