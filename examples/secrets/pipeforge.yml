name: env-secrets

secrets:
  DEMO_TOKEN:
    from: env
    required: true

pipeline:
  - name: Mask secret output
    env:
      TOKEN: "${secrets.DEMO_TOKEN}"
    # Demonstration only; applications should avoid printing secrets.
    script: python -c 'import os; print("Token:", os.environ["TOKEN"])'
